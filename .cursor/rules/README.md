# Cursor rules (IDE integration)

Cursor auto-loads `.mdc` files in this folder during editing sessions.

**Canonical source:** [`docs/ui-rules/README.md`](../../docs/ui-rules/README.md)

Edit rules in `docs/ui-rules/` first, then mirror changes here if needed.
